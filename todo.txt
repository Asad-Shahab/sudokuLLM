Download CUDA 12.6 but without the nightly stuff

install pip 
python -m pip --version

curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py

python get-pip.py

python -m pip --version

uninstall torch and resintall:
pip uninstall torch torchvision torch audio

pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126

Insatll triton:
pip install https://huggingface.co/madbuda/triton-windows-builds/resolve/main/triton-3.0.0-cp311-cp311-win_amd64.whl

git clone https://github.com/Asad-Shahab/sudoku_finetuning.git
cd sudoku_finetuning

pip install -r requirements.txt
pip --cache-dir pipcache/ install -r sudoku_finetuning/requirements.txt

python finetune.py


tmux new -s session_name

to detach: Ctrl + b  then  d

reconnect: tmux attach -t session_name

kill: tmux kill-session -t session_name

